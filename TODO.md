# TODO

- [x] Add less-style `/` search with `n` / `N` inside lists and session briefs.

Keep the default browser streaming and cache-free. Any future cache must be
explicit, local, invalidated by file metadata, and justified by a measured
interactive slowdown.
