name: test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f tests/Dockerfile -t asdu-smoke .
      - run: docker run --rm --network none asdu-smoke
